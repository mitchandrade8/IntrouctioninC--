
#include <iostream>
using namespace std;

int main() {


    return 0;

}

// MARK: - Else If vs Switch Case 
    /// 'Switch Case' can only check for equality (num == 5), so if you need to check for a range of values
    /// (num > 50 && num <= 60), use else If instead.

